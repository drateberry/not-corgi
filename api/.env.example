# Copy to .env and fill in. .env is gitignored; this template is committed.

# Path to the trained model produced by model/src/train.py
MODEL_PATH=artifacts/not_corgi.keras

# Flask
HOST=0.0.0.0
PORT=5001
