/**
 * Results screen.
 *
 * Per specifications.md section 4.3, this must show:
 *   - a prominent verdict (Corgi / Not Corgi) — the "Not Hotdog" homage lands here
 *   - the specific breed determination
 *   - all three class probabilities
 *   - a toggleable Grad-CAM heatmap overlay
 *
 * TODO:
 *   - Render the verdict large and unmistakable; it is what the demo video shows.
 *   - Show all three probabilities, not just the winner — the fine-grained
 *     Pembroke/Cardigan distinction is the point of the project, and a close
 *     call between the two is more honest and more interesting than a bare label.
 *   - Toggle between the original photo and the Grad-CAM overlay.
 *   - Route back to the camera for another shot.
 */

export default function ResultScreen() {
  // TODO: implement.
  return null;
}
