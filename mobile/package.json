{
  "name": "not-corgi",
  "version": "1.0.0",
  "main": "App.js",
  "scripts": {
    "start": "expo start",
    "ios": "expo start --ios",
    "android": "expo start --android"
  },
  "dependencies": {
    "COMMENT": "TODO: generate this properly with `npx create-expo-app` rather than hand-editing, then add expo-camera and expo-image-picker via `npx expo install` so versions match the SDK."
  },
  "private": true
}
