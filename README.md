# Not Corgi

**Video demo:** TODO — paste the unlisted/public YouTube URL here before submission.

TODO — write the user's manual. Per `instructions.md` this must be several paragraphs
and must leave staff with no follow-up questions. Suggested sections to fill in:

- What the project is and what problem it solves
- Repository layout (`model/`, `api/`, `mobile/`)
- Prerequisites (Python version, Node version, Expo Go on a physical phone)
- How to assemble the dataset and train the model
- How to run the Flask API locally, and where it is deployed
- How to run the mobile app and point it at the API
- How to run the tests
- Known limitations (e.g. the tail-occlusion accuracy ceiling)
